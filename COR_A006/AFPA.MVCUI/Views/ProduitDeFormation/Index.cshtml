@model IEnumerable<AFPA.BOL.ProduitDeFormation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DesignationProduitFormation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DesignationProduitFormation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdProduitFormation }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdProduitFormation }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdProduitFormation })
        </td>
    </tr>
}

</table>
